services:
  - type: web
    name: amazon-recommendation-system
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -c "import nltk; nltk.download('stopwords'); nltk.download('wordnet')"
      python export_similarity_matrix.py
    startCommand: gunicorn app:app
    repo: https://github.com/arham2003/Amazon-Product-Recommender-System # Replace with your actual GitHub repo URL
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: FLASK_ENV
        value: production
    plan: free
    healthCheckPath: /
    autoDeploy: true 